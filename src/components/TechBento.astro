---
import { Cpu, Binary, FastForward, GitBranch } from 'lucide-react';
---

<section class="py-24 bg-[#030303]">
  <div class="max-w-7xl mx-auto px-6">
    <div class="grid grid-cols-1 md:grid-cols-4 gap-4 auto-rows-[240px]">
      <div class="md:col-span-2 bg-dark-surface border border-white/5 rounded-3xl p-8 flex flex-col justify-between">
        <Binary className="text-brand-primary w-10 h-10" />
        <div>
          <h3 class="text-2xl font-bold text-white mb-2">Single Binary Engine</h3>
          <p class="text-zinc-500 text-sm italic">Built with Go for maximum performance. No Node_modules, no heavy runtimes. Just download and run.</p>
        </div>
      </div>

      <div class="bg-dark-surface border border-white/5 rounded-3xl p-8 flex flex-col justify-between">
        <Cpu className="text-zinc-400 w-8 h-8" />
        <h3 class="text-lg font-bold text-white leading-tight italic">Low Footprint <br/><span class="text-zinc-500 text-xs font-normal">Under 32MB RAM usage</span></h3>
      </div>

      <div class="bg-dark-surface border border-white/5 rounded-3xl p-8 flex flex-col justify-between">
        <GitBranch className="text-zinc-400 w-8 h-8" />
        <h3 class="text-lg font-bold text-white leading-tight italic">Plugin Ready <br/><span class="text-zinc-500 text-xs font-normal">Extend with custom drivers</span></h3>
      </div>

      <div class="md:col-span-4 bg-linear-to-r from-zinc-900 to-black border border-white/5 rounded-3xl p-8 flex items-center gap-12">
        <div class="hidden md:block shrink-0">
           <div class="w-16 h-16 rounded-full bg-brand-primary/20 flex items-center justify-center">
              <FastForward className="text-brand-primary w-8 h-8" />
           </div>
        </div>
        <div>
          <h3 class="text-2xl font-bold text-white mb-2 italic tracking-tighter">Real-time Provider Polling</h3>
          <p class="text-zinc-500 max-w-2xl text-sm leading-relaxed">
            Our multi-threaded sync engine fetches status from Cloudflare, GoDaddy, and AWS simultaneously. 
            Get notified of DNS changes or expiry alerts within seconds of them occurring.
          </p>
        </div>
      </div>
    </div>
  </div>
</section>