---
import { ArrowRight, Globe, Shield, Activity } from 'lucide-react';
---

<section class="relative pt-32 pb-20 overflow-hidden">
  <div class="absolute top-0 left-1/2 -translate-x-1/2 w-200 h-125 glow-bg -z-10 rounded-full blur-3xl opacity-50"></div>

  <div class="max-w-7xl mx-auto px-6 text-center">
    <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full border border-brand-primary/30 bg-brand-primary/10 text-brand-primary text-xs font-medium mb-6">
      <span class="relative flex h-2 w-2">
        <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-brand-primary opacity-75"></span>
        <span class="relative inline-flex rounded-full h-2 w-2 bg-brand-primary"></span>
      </span>
      v1.0 is now live
    </div>

    <h1 class="text-5xl md:text-7xl font-bold tracking-tight text-white mb-6">
      Manage Domains <br />
      <span class="text-transparent bg-clip-text bg-linear-to-r from-brand-primary to-purple-400">With Open Source Freedom</span>
    </h1>
    
    <p class="text-xl text-zinc-400 max-w-2xl mx-auto mb-10">
      The production-grade, self-hosted domain name manager. Monitor expiry, manage DNS records, and automate SSL renewals in one unified dashboard.
    </p>

    <div class="flex flex-col sm:flex-row items-center justify-center gap-4 mb-20">
      <a href="#" class="h-12 px-8 rounded-full bg-brand-primary hover:bg-brand-secondary text-white font-medium flex items-center gap-2 transition-all shadow-lg shadow-brand-primary/25">
        Deploy Now <ArrowRight className="w-4 h-4" />
      </a>
      <a href="#" class="h-12 px-8 rounded-full border border-dark-border hover:bg-dark-surface text-white font-medium flex items-center gap-2 transition-all">
        Read Documentation
      </a>
    </div>

    <div class="relative max-w-5xl mx-auto rounded-xl border border-dark-border bg-dark-surface/50 backdrop-blur-xl shadow-2xl overflow-hidden">
      <div class="h-10 border-b border-dark-border bg-dark-surface flex items-center px-4 gap-2">
        <div class="w-3 h-3 rounded-full bg-red-500/80"></div>
        <div class="w-3 h-3 rounded-full bg-yellow-500/80"></div>
        <div class="w-3 h-3 rounded-full bg-green-500/80"></div>
        <div class="ml-4 px-3 py-1 bg-dark-bg rounded text-xs text-zinc-500 font-mono w-64">opendnm.local/dashboard</div>
      </div>

      <div class="p-6 grid gap-6 text-left">
        <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
          <div class="p-4 rounded-lg bg-dark-bg border border-dark-border">
            <div class="flex items-center gap-3 text-zinc-400 mb-2">
              <Globe className="w-4 h-4" /> <span>Total Domains</span>
            </div>
            <div class="text-2xl font-bold">142</div>
          </div>
          <div class="p-4 rounded-lg bg-dark-bg border border-dark-border">
            <div class="flex items-center gap-3 text-zinc-400 mb-2">
              <Shield className="w-4 h-4" /> <span>SSL Status</span>
            </div>
            <div class="text-2xl font-bold text-emerald-400">ALL VALID</div>
          </div>
          <div class="p-4 rounded-lg bg-dark-bg border border-dark-border">
            <div class="flex items-center gap-3 text-zinc-400 mb-2">
              <Activity className="w-4 h-4" /> <span>Uptime</span>
            </div>
            <div class="text-2xl font-bold">99.99%</div>
          </div>
        </div>

        <div class="rounded-lg border border-dark-border overflow-hidden">
          <div class="bg-dark-surface px-4 py-3 border-b border-dark-border text-xs font-semibold text-zinc-400 uppercase tracking-wider flex justify-between">
            <span>Domain Name</span>
            <span>Status</span>
          </div>
          <div class="bg-dark-bg divide-y divide-dark-border">
            {['production-app.com', 'blog-service.io', 'internal-tools.net'].map((domain, i) => (
              <div class="px-4 py-3 flex items-center justify-between group hover:bg-white/5 transition-colors">
                <div class="flex items-center gap-3">
                  <div class="w-2 h-2 rounded-full bg-emerald-500 shadow-[0_0_8px_rgba(16,185,129,0.5)]"></div>
                  <span class="text-sm font-medium text-zinc-200">{domain}</span>
                </div>
                <div class="text-xs text-zinc-500 font-mono">Expires in {120 - i * 10} days</div>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  </div>
</section>